# NaNoBananacomic 계획 진행현황

이 문서는 현재 구현 상태를 계획 문서와 대조해 추적하는 체크리스트다.

- 기준 문서: [계획.md](/mnt/c/Users/pjjpj/Desktop/NaNoBananacomic/doc/계획.md)
- 구현 기준: [구현계획.md](/mnt/c/Users/pjjpj/Desktop/NaNoBananacomic/doc/구현계획.md)
- 운영 규칙: [rules.md](/mnt/c/Users/pjjpj/Desktop/NaNoBananacomic/doc/rules.md)

## 현재 판정

- 기준일: `2026-04-03`
- Phase 1: 운영용 1차 완료
- Phase 1.5: 운영용 1차 완료
- Phase 2: 운영용 1차 완료
- Phase 3: 미착수
- Phase 4: 미착수
- Phase 5: 미착수

## Phase 1 체크리스트

- [x] `legacy/` 격리 후 새 파이프라인을 `src/` 기준으로 재구성
- [x] `project.json` / `pages/*.page.json` 저장 계층 작성
- [x] detector adapter 계층 작성
- [x] 공개 detector 2개 이상을 동일 인터페이스로 연결
- [x] union mask 생성
- [x] overlay preview 생성
- [x] 페이지별 detector 기록
- [x] baseline contour detector를 production detector와 구분
- [x] profile router 작성
- [x] `auto` detect가 프로파일별 detector를 고르도록 연결
- [x] `testmanga/` 5장에 대해 Step 1 detect -> validate-step1 재검증

메모:

- 현재 공개 detector 연결 상태
  - `manga109_seg_v1`
  - `kitsumed_seg_v1`
  - `ogkalu_bubble_v1`
- `contour_baseline_v1`는 비교/비상용 baseline으로만 유지

## Phase 1.5 체크리스트

- [x] `benchmark-detectors` CLI 작성
- [x] `benchmark_detection.py` 작성
- [x] benchmark 결과 JSON 저장
- [x] benchmark 결과 Markdown 표 저장
- [x] 흑백/컬러 혼합 샘플 5장에 대해 공개 모델 비교
- [x] 흑백 일본 만화 primary detector 결정
- [x] 컬러/3D 만화 primary detector 결정
- [x] fallback 필요 여부 결정

결정 사항:

- 흑백 일본 만화 primary: `manga109_seg_v1`
- 컬러/3D 만화 primary: `ogkalu_bubble_v1`
- 비교군: `kitsumed_seg_v1`
- fallback 필요 여부: 필요
- fallback 방향: `Grounded SAM 2` 또는 `SAM 2` refine를 차후 Phase 4에서 연결

주의:

- 현재 benchmark는 GT 라벨 기반 정식 benchmark가 아니라 `proxy benchmark without ground truth`다.
- 따라서 현재 판정은 “운영용 1차 채택 완료”이며, 향후 GT 데이터가 생기면 재평가한다.
- 최신 mixed sample 5장 proxy benchmark의 수치상 winner는 `ogkalu_bubble_v1`였지만, 현재 운영 라우팅 정책은 시각 검토를 반영해 `bw_manga -> manga109_seg_v1`, `color/3D -> ogkalu_bubble_v1`로 유지한다.

## Phase 2 체크리스트

- [x] `make_balloons_only.py` 작성
- [x] `comic-pipeline make-layer` CLI 작성
- [x] `comic-pipeline validate-step2` CLI 작성
- [x] `balloons_only_rgba_path` 저장 및 `nano_pending` 상태 전이 연결
- [x] soft alpha edge 생성 로직 추가
- [x] outside-support alpha zero 검증 추가
- [x] outside-support RGB zero 검증 추가
- [x] synthetic detect -> make-layer -> validate-step2 테스트 추가
- [x] `testmanga/` 5장에 대해 Step 2 smoke test 실행

메모:

- 현재 Step 2는 OpenCV 기반 deterministic 후처리만 사용한다.
- support 바깥 alpha와 RGB는 모두 0으로 강제한다.
- tiny balloon에서도 erosion 때문에 alpha가 비지 않도록 fallback을 넣었다.
- `0002_0001` 레이어는 수동 sanity check로 확인했다.

## CUDA / GPU 상태

최근 확인 시점: `2026-04-03`

- [x] `/dev/dxg` 확인
- [x] `nvidia-smi` 정상 동작
- [x] `torch.cuda.is_available() == True`
- [x] `torch.cuda.get_device_name(0)` 확인
- [x] detector 추론 후 GPU 메모리 증가 확인

확인 결과:

- GPU: `NVIDIA GeForce RTX 4070 SUPER`
- PyTorch: `2.11.0+cu130`
- CUDA runtime reported by torch: `13.0`

## 최근 산출물

- 최신 benchmark 결과표:
  - [benchmark_results.md](/mnt/c/Users/pjjpj/Desktop/NaNoBananacomic/testmanga/artifacts/benchmarks/2026-04-03T051722/benchmark_results.md)
- 최신 Step 2 레이어 예시:
  - [0002_0001_balloons_only.png](/mnt/c/Users/pjjpj/Desktop/NaNoBananacomic/testmanga/artifacts/layers/0002_0001_balloons_only.png)
- 샘플 프로젝트:
  - [project.json](/mnt/c/Users/pjjpj/Desktop/NaNoBananacomic/testmanga/project.json)

## 다음 우선순위

- [ ] Phase 3 Nano Banana 입력/출력 계약 확정
- [ ] Nano raw 수용 경로와 파일 계약 구현
- [ ] Step 2 checkerboard preview 또는 GUI 미리보기 연결
- [ ] `uv` 기반 실행/검증 경로 정리
- [ ] Grounded SAM 2 fallback 조사 및 연결 계획 구체화
